query getAllLaunches {
    getAllLaunches{
        id
        name
        links {
            patch {
                large
            }
        }
        launchpad
        rocket {
            name
        }
        dateUnix
    }
}
