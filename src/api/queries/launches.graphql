query getAllLaunches {
    getAllLaunches{
        id
        name
        links {
            patch {
                large
            }
        }
        launchpad
        rocket {
            name
        }
        dateUtc
    }
}

query getLaunchById($id:String!){
    getLaunchById(id: $id){
        name
    }
}
